{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {},
  "postCreateCommand": "chmod +x ~\\.bash_profile ~\\.bashrc ~\\.inputrc && apt update -y && apt install -y inetutils-ping"
}
